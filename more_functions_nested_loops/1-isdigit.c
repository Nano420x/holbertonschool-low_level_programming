#include "main.h"

/**
* _isdigit- il fais des trucs plutot pas mal
* @c: int le c pour qu'il vaux un truc
* Return: Always 0 (Success)
*/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
return (0);
}
